<div id="sidebar">
  <ul class="site-nav">
    {% for link in linklists[section.settings.collection_linklist].links %}
      {%- assign child_list_handle = link.title | handleize -%}
      {% if menus[child_list_handle].links != blank %}
        <li class="site-nav--has-submenu{% if link.active %} site-nav--active{% endif %}">
          <a href="{{ link.url }}" class="site-nav__link">
            {{ link.title }}
          </a>
          <ul class="site-nav__submenu">
            {% for childlink in menus[child_list_handle].links %}
              <li {% if childlink.active %}class="site-nav--active"{% endif %}>
                <a href="{{ childlink.url }}" class="site-nav__link">{{ childlink.title | escape }}</a>
              </li>
            {% endfor %}
          </ul>
        </li>
      {% else %}
        <li {% if link.active %}class="site-nav--active"{% endif %}>
          <a href="{{ link.url }}" class="site-nav__link">{{ link.title }}</a>
        </li>
      {% endif %}
    {% endfor %}
    <li>&nbsp;</li>
    {% for link in linklists[section.settings.main_linklist].links %}
      {%- assign child_list_handle = link.title | handleize -%}
      {% if menus[child_list_handle].links != blank %}
        <li class="site-nav--has-submenu{% if link.active %} site-nav--active{% endif %}">
          <a href="{{ link.url }}" class="site-nav__link">
            {{ link.title }}
          </a>
          <ul class="site-nav__submenu">
            {% for childlink in menus[child_list_handle].links %}
              <li {% if childlink.active %}class="site-nav--active"{% endif %}>
                <a href="{{ childlink.url }}" class="site-nav__link">{{ childlink.title | escape }}</a>
              </li>
            {% endfor %}
          </ul>
        </li>
      {% else %}
        <li {% if link.active %}class="site-nav--active"{% endif %}>
          <a href="{{ link.url }}" class="site-nav__link">{{ link.title }}</a>
        </li>
      {% endif %}
    {% endfor %}
    <ul class="site-nav">
      {% for link in linklists[section.settings.social_linklist].links %}
        {%- assign child_list_handle = link.title | handleize -%}
        {% if menus[child_list_handle].links != blank %}
          <li class="site-nav--has-submenu{% if link.active %} site-nav--active{% endif %}">
            <a href="{{ link.url }}" class="site-nav__link">
              {{ link.title }}
            </a>
            <ul class="site-nav__submenu">
              {% for childlink in menus[child_list_handle].links %}
                <li {% if childlink.active %}class="site-nav--active"{% endif %}>
                  <a href="{{ childlink.url }}" class="site-nav__link">{{ childlink.title | escape }}</a>
                </li>
              {% endfor %}
            </ul>
          </li>
        {% else %}
          <li {% if link.active %}class="site-nav--active"{% endif %}>
            <a href="{{ link.url }}" class="site-nav__link">{{ link.title }}</a>
          </li>
        {% endif %}
      {% endfor %}
    </ul>
  </ul>

<ul class="site-nav">
 <form id="subscribe-form" action="https://rudigernreich.us18.list-manage.com/subscribe/post-json?u=3f4382ce9ff0dc514dc0911f1&amp;id=879e3f2550" method="get">
    <div id="mc_embed_signup_scroll">

    <div id="mce-responses" class="clear">
        <div class="response" id="mce-error-response" style="display: none;">
        </div>
        <div class="response" id="mce-success-response" style="display: none;">
        </div>
    </div>

    <div style="position: absolute; left: -5000px;" aria-hidden="true">
    <input type="text" name="b_3f4382ce9ff0dc514dc0911f1_879e3f2550" tabindex="-1" value="">
    </div>

    <div id="subscribe-box">
        <div id="subscribe-result">
            sign up
        </div>

        <div id="email-input">
            <input type="email" placeholder="email" value="" name="EMAIL" autocomplete="off">
        </div>

        <div id="submit-button">
            <input type="submit" value="GO" name="subscribe" id="change-border">
        </div>
    </div>

    </div>
</form>
</div>
  </ul>
</div>

<div data-section-id="{{ section.id }}" data-section-type="header-section">
  <header id="site-header" class="site-header" role="banner">
    <nav role="navigation">
      <a href="#" class="mobile-menu-link" data-toggle="body" id="sidebar-toggle">
        <span class="span-hamburger">{% include 'icon-hamburger' %}</span>
        <span class="span-close hidden">{% include 'icon-close' %}</span>
      </a>
    </nav>

    {% if template.name == 'index' %}
      <h1 itemscope itemtype="http://schema.org/Organization">
    {% else %}
      <div class="h1" itemscope itemtype="http://schema.org/Organization">
    {% endif %}
        <a href="/" itemprop="url" class="site-logo{% if section.settings.logo != blank %} site-header__logo-image{% endif %}">
          {% if section.settings.logo != blank %}
            <img src="{{ section.settings.logo | img_url: '600x600' }}"

                 alt="{{ section.settings.logo.alt | default: shop.name }}"
                 itemprop="logo">
          {% else %}
            <h1>{{ shop.name }}</h1>
          {% endif %}
        </a>
    {% if template.name == 'index' %}
      </h1>
    {% else %}
      </div>
    {% endif %}

    <a class="cart-link" href="/cart">{% include 'icon-cart' %}{% if cart.item_count != 0 %}<div class="cart-full"></div>{% endif %}</a>

    {% comment %}{% if shop.customer_accounts_enabled %}
      <ul>
        {% if customer %}
          <li>
            {% if customer.first_name != blank %}
              {% capture first_name %}<a href="/account">{{ customer.first_name }}</a>{% endcapture %}
              {{ 'layout.customer.logged_in_as_html' | t: first_name: first_name }}
            {% else %}
              <a href="/account">{{ 'layout.customer.account' | t }}</a>
            {% endif %}
          </li>
          <li>
            {{ 'layout.customer.log_out' | t | customer_logout_link }}
          </li>
        {% else %}
          <li>
            {{ 'layout.customer.log_in' | t | customer_login_link }}
          </li>
          <li>
            {{ 'layout.customer.create_account' | t | customer_register_link }}
          </li>
        {% endif %}
      </ul>
    {% endif %}{% endcomment %}
    <div class="cookie-notification" style="display:none;">
      By continuing to use this website, you accept cookies that we use to improve your experience on this site and to provide you with services and content tailored to your interests. To learn more about cookies, including how to disable them please read our <a href="#">Privacy and Cookie Policy</a>. <a class="close-link" href="#">OK</a>
    </div>
  </header>
</div>

{% if section.settings.intro_enable == true %}
  {% include 'intro' %}
{% endif %}

{% javascript %}

    $('.close-link').click(function(e){
      localStorage.setItem('accepted',true);
        $('.cookie-notification').hide();
    });

    var is_accepted = localStorage.getItem('accepted');

    if(is_accepted){
      console.log('localStorage')
      $('.cookie-notification').hide();
    }

    if(!is_accepted){
      console.log('no localStorage');
      $('.cookie-notification').show();
    }

{% endjavascript %}
{% schema %}
  {
    "name": "Header",
    "settings": [
      {
        "type": "image_picker",
        "id": "logo",
        "label": "Logo image"
      },
      {
        "type": "link_list",
        "id": "main_linklist",
        "label": "Menu",
        "default": "main-menu"
      },
      {
        "type": "link_list",
        "id": "collection_linklist",
        "label": "Collection Menu",
        "default": "main-menu"
      },
      {
        "type": "link_list",
        "id": "social_linklist",
        "label": "Social Menu",
        "default": "main-menu"
      },
      {
         "type":      "header",
         "content":   "Intro",
         "info":      "Display an image or a video on the landing page."
      },
      {
        "type": "checkbox",
        "id": "intro_enable",
        "label": "Enable Intro"
      },
      {
        "type": "image_picker",
        "id": "intro_img",
        "label": "Intro Image"
      },
      {
        "type": "text",
        "id": "intro_video",
        "label": "Video URL"
      },
      {
        "type": "textarea",
        "id": "headline_text",
        "label": "Headline Text"
      }
    ]
  }
{% endschema %}
